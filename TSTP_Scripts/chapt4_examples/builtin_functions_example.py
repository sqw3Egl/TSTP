# functions that are built in to Python

# print

len("Monty") #returns the length (number of characters used)

len("Python")

str(100) #takes an object and converts/returns it to the string(str) data type.

int("1") #takes an object and converts/returns it to the integar(int) data type.

float("100") #takes an object and converts/returns it to the float(decimal) data type.

